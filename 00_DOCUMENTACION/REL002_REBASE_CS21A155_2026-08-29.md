# CS21A155 · REL-002 reconstruido sobre main actual

Fecha: 2026-08-29 · Costa Rica

## Base

- `main`: `53df524d0a9eab867d3b307b3e633f366af92a63`
- Rama moderna: `fix/rel002-ventas-auth-scope-current-cs21a155`
- PR histórico fuente: #113 (`fix/rel002-ventas-auth-scope`), 93 commits detrás de `main` al momento de la reconstrucción.

## Decisión

No se fusionó ni rebajó la rama histórica completa. Se reconstruyó exclusivamente su delta funcional vigente sobre el `main` actual mediante preimágenes exactas y conteos obligatorios.

## Delta funcional verificado

- `src/ventas_data.jsx`: +3/-2
  - `postVentas(payload = {})`;
  - obtiene el token actual de Campus;
  - serializa `{ ...payload, token }`.
- `src/ventas_dashboard.jsx`: +2/-2
  - `MiMatriculasMes` recibe `scopeAsesor`;
  - `ProspectoDrawer` recibe `scopeAsesor`.

Antes de añadir QA/documentación permanente, la comparación contra `main` confirmó exactamente 2 archivos funcionales y +5/-4.

## Invariantes

1. El token de sesión prevalece sobre cualquier campo `token` incluido por un caller.
2. Dashboard, matrículas mensuales y drawer deben compartir el mismo scope comercial.
3. No se amplían roles de acceso de Ventas.
4. No se reactiva el trigger legacy directo `activarEstudiante` desde el drawer.
5. No se toca Apps Script, pagos, ACL ni producción.

## QA permanente

- `scripts/qa_rel002_sales_auth_cs21a155.mjs`
- `.github/workflows/qa-rel002-sales-auth-cs21a155.yml`

## Evidencia pendiente

E2 autenticada no productiva:
- rol ventas ejecuta mutaciones permitidas sin `sesion_requerida`;
- ventas no puede operar prospectos ajenos;
- ventas no puede activar estudiantes;
- admin/superadmin al usar `Ver como asesor` conserva el mismo scope en dashboard, matrículas y drawer.

## Estado

**SOURCE FIXED ON CURRENT MAIN · STATIC QA · RUNTIME E2/E3 PENDING · NO PROD**
